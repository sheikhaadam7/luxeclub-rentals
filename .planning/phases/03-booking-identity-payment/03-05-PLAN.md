---
phase: 03-booking-identity-payment
plan: "05"
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - components/email/BookingConfirmationEmail.tsx
  - app/(protected)/bookings/page.tsx
  - app/(protected)/bookings/[bookingId]/page.tsx
  - app/actions/bookings.ts
  - app/(protected)/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "User receives a booking confirmation email after successful payment"
    - "User can view a booking confirmation page with full booking details"
    - "User can see a list of upcoming and past bookings at /bookings"
    - "Booking confirmation email contains vehicle name, dates, pricing breakdown, and booking status"
    - "Dashboard shows a link to My Bookings"
  artifacts:
    - path: "components/email/BookingConfirmationEmail.tsx"
      provides: "React Email template for booking confirmation"
      min_lines: 30
    - path: "app/(protected)/bookings/page.tsx"
      provides: "My Bookings list page showing upcoming and past bookings"
      min_lines: 30
    - path: "app/(protected)/bookings/[bookingId]/page.tsx"
      provides: "Single booking detail/confirmation page"
      min_lines: 40
  key_links:
    - from: "app/actions/bookings.ts"
      to: "lib/email/send.ts"
      via: "Sends confirmation email after booking creation"
      pattern: "sendEmail|BookingConfirmationEmail"
    - from: "app/(protected)/bookings/page.tsx"
      to: "bookings"
      via: "Queries user's bookings from Supabase ordered by start_date"
      pattern: "from.*bookings"
    - from: "app/(protected)/bookings/[bookingId]/page.tsx"
      to: "bookings"
      via: "Fetches single booking with vehicle join"
      pattern: "from.*bookings.*select"
---

<objective>
Build the post-booking experience: a React Email confirmation template sent via Resend after successful payment, a booking detail/confirmation page showing full booking info, a My Bookings list page showing upcoming and past bookings, and wiring the email send into the createBooking flow.

Purpose: This completes the booking loop from the user's perspective. After paying, they see a confirmation page, receive an email, and can always return to view their bookings. This satisfies BOOK-07 (email + in-app confirmation) and BOOK-08 (view bookings).

Output: Booking confirmation email template, /bookings list page, /bookings/[bookingId] detail page, email sending wired into createBooking.
</objective>

<execution_context>
@C:/Users/humza/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/humza/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-booking-identity-payment/03-RESEARCH.md
@.planning/phases/03-booking-identity-payment/03-01-SUMMARY.md
@.planning/phases/03-booking-identity-payment/03-04-SUMMARY.md
@lib/email/send.ts
@app/actions/bookings.ts
@app/(protected)/dashboard/page.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Booking confirmation email template and email sending integration</name>
  <files>
    components/email/BookingConfirmationEmail.tsx
    app/actions/bookings.ts
  </files>
  <action>
1. Create `components/email/BookingConfirmationEmail.tsx` using @react-email/components:
   - Import: `Html, Head, Body, Container, Section, Text, Heading, Hr, Img` from `@react-email/components`
   - Props interface: `{ booking: { id, vehicleName, vehicleImage, startDate, endDate, durationType, pickupMethod, returnMethod, deliveryAddress, depositChoice, rentalSubtotal, deliveryFee, returnFee, noDepositSurcharge, depositAmount, totalDue, paymentMethod, status } }`
   - Template structure:
     - Dark background matching brand (`backgroundColor: '#000000'`)
     - LuxeClub heading in Playfair Display-like serif font (use Georgia as fallback — email clients don't support Google Fonts)
     - "Booking Confirmed" heading
     - Vehicle name and primary image (if available)
     - Booking details section: dates (formatted as "Feb 20 - Feb 25, 2026"), duration type, pickup method, return method, delivery address (if delivery)
     - Pricing breakdown table:
       - Rental subtotal
       - Delivery fee (if > 0)
       - Return fee (if > 0)
       - No-deposit surcharge (if > 0)
       - Total due (bold)
       - Deposit hold (if applicable, with note "Pre-authorized, not charged")
     - Payment method badge
     - Booking reference: booking ID (truncated UUID)
     - Footer: "LuxeClub Rentals, Downtown Dubai" and unsubscribe link
   - Style: inline styles only (email client compatibility), dark theme with white text, cyan accents for headings

2. Update `app/actions/bookings.ts` — add email sending to createBooking:
   - After successful booking insert, fetch the user's email from supabase.auth.getUser()
   - Import `sendEmail` from `@/lib/email/send`
   - Import `BookingConfirmationEmail` from `@/components/email/BookingConfirmationEmail`
   - Call `sendEmail({ to: userEmail, subject: 'Booking Confirmed - {vehicleName}', react: BookingConfirmationEmail({ booking: { ...bookingData, vehicleName, vehicleImage } }) })`
   - Wrap email send in try/catch — email failure should NOT fail the booking (log error but still return success)
   - Also export `getUserBookings()` Server Action:
     - Get authenticated user
     - Query bookings where user_id = user.id, joined with vehicles (name, slug, primary_image_url)
     - Order by start_date descending
     - Return array of bookings with vehicle info
   - Also export `getBookingDetail(bookingId: string)` Server Action:
     - Get authenticated user
     - Query single booking by id where user_id = user.id (RLS also enforces this)
     - Join with vehicles for name, slug, primary_image_url, daily_rate
     - Return booking with vehicle info, or null if not found
  </action>
  <verify>
    `npx tsc --noEmit` passes. BookingConfirmationEmail renders a valid React Email template. createBooking now sends confirmation email (in try/catch). getUserBookings and getBookingDetail are exported.
  </verify>
  <done>
    React Email confirmation template with dark luxury branding, pricing breakdown, and booking details. Email sending wired into createBooking (failure-tolerant). getUserBookings and getBookingDetail Server Actions exported for the bookings pages.
  </done>
</task>

<task type="auto">
  <name>Task 2: My Bookings list page, booking detail page, and dashboard navigation</name>
  <files>
    app/(protected)/bookings/page.tsx
    app/(protected)/bookings/[bookingId]/page.tsx
    app/(protected)/dashboard/page.tsx
  </files>
  <action>
1. Create `app/(protected)/bookings/page.tsx` — Server Component:
   - Call `getUserBookings()` to fetch all user bookings
   - Split into two sections: "Upcoming" (start_date >= today, status not 'cancelled' or 'completed') and "Past" (start_date < today or status is 'completed'/'cancelled')
   - Each booking card shows:
     - Vehicle primary image (small thumbnail via next/image, 80x60)
     - Vehicle name
     - Date range: "Feb 20 - Feb 25, 2026"
     - Duration type badge (Daily/Weekly/Monthly)
     - Status badge with color coding: confirmed=brand-cyan, pending=yellow, completed=green, cancelled=red, pending_cash=amber
     - Payment status: paid/pending_cash/unpaid
     - Total: "AED {totalDue}"
     - Link to `/bookings/{bookingId}` for details
   - Empty state: "No bookings yet. Browse our collection to find your perfect ride." with link to /catalogue
   - Dark luxury styling: bg-luxury, cards with bg-brand-surface, border-brand-border, hover effect

2. Create `app/(protected)/bookings/[bookingId]/page.tsx` — Server Component:
   - Accept `params: Promise<{ bookingId: string }>` (Next.js 16 pattern)
   - Call `getBookingDetail(bookingId)` to fetch booking
   - 404 if not found
   - Show full booking details:
     - Vehicle image (hero, 16:9 aspect ratio)
     - Vehicle name and link to catalogue page
     - "Booking Confirmed" or status heading
     - Booking reference (first 8 chars of UUID)
     - Date and time details
     - Pickup method (delivery address shown if delivery)
     - Return method
     - Full pricing breakdown (same layout as PriceSummary):
       - Rental subtotal, delivery fee, return fee, no-deposit surcharge, total
       - Deposit hold amount (if applicable)
     - Payment method and status
     - Cancellation policy section (same text as StepPayment)
   - Dark luxury styling consistent with rest of app
   - Back link to /bookings

3. Update `app/(protected)/dashboard/page.tsx`:
   - Add a "My Bookings" navigation card alongside the existing "Explore Our Collection" card
   - Style: bg-brand-surface border border-brand-border rounded-[--radius-card] p-6
   - Icon suggestion: calendar or car icon
   - Link to /bookings
   - Read existing dashboard code first to maintain the exact same patterns and styling
  </action>
  <verify>
    `npx tsc --noEmit` passes. `/bookings` page renders with upcoming and past sections. `/bookings/{id}` page shows full booking details. Dashboard has "My Bookings" navigation card. Empty state shows when user has no bookings.
  </verify>
  <done>
    My Bookings page at /bookings shows upcoming and past bookings with status badges, vehicle info, and pricing. Booking detail page at /bookings/{id} shows full confirmation with pricing breakdown, dates, delivery info, and cancellation policy. Dashboard has "My Bookings" navigation card.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero TypeScript errors
- BookingConfirmationEmail renders valid React Email template
- createBooking sends confirmation email (non-blocking on failure)
- /bookings page shows upcoming and past sections with correct filtering
- /bookings/{id} shows complete booking detail with pricing breakdown
- Dashboard has "My Bookings" card linking to /bookings
- Empty state renders when no bookings exist
</verification>

<success_criteria>
After completing a booking, the user receives a confirmation email with full booking details and pricing breakdown in dark luxury branding. The user can view their booking at /bookings/{id} with all configuration details. The /bookings page shows all upcoming and past bookings with status badges. The dashboard provides navigation to both the catalogue and bookings.
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-identity-payment/03-05-SUMMARY.md`
</output>
