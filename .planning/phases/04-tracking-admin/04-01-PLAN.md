---
phase: 04-tracking-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260220300000_tracking_admin_phase4.sql
  - app/api/gps/route.ts
  - lib/hooks/use-realtime-booking.ts
  - lib/hooks/use-vehicle-location.ts
autonomous: true

must_haves:
  truths:
    - "Booking status CHECK constraint allows car_on_the_way and car_delivered values"
    - "vehicle_locations table exists with RLS and Realtime enabled"
    - "GPS ingest endpoint accepts POST with device_id, lat, lng and upserts into vehicle_locations"
    - "Realtime hook receives booking status changes pushed by Supabase"
    - "Realtime hook receives vehicle location changes pushed by Supabase"
  artifacts:
    - path: "supabase/migrations/20260220300000_tracking_admin_phase4.sql"
      provides: "DB schema: vehicle_locations, extended booking status CHECK, gps_device_id, is_active, vehicle_availability_blocks"
      contains: "car_on_the_way"
    - path: "app/api/gps/route.ts"
      provides: "GPS hardware ingest POST endpoint"
      exports: ["POST"]
    - path: "lib/hooks/use-realtime-booking.ts"
      provides: "Client hook subscribing to booking status changes via Supabase Realtime"
      exports: ["useRealtimeBooking"]
    - path: "lib/hooks/use-vehicle-location.ts"
      provides: "Client hook subscribing to vehicle location updates via Supabase Realtime"
      exports: ["useVehicleLocation"]
  key_links:
    - from: "app/api/gps/route.ts"
      to: "vehicle_locations table"
      via: "admin client upsert on vehicle_id PK"
      pattern: "upsert.*vehicle_locations"
    - from: "lib/hooks/use-vehicle-location.ts"
      to: "vehicle_locations table"
      via: "postgres_changes UPDATE subscription"
      pattern: "postgres_changes.*vehicle_locations"
    - from: "lib/hooks/use-realtime-booking.ts"
      to: "bookings table"
      via: "postgres_changes UPDATE subscription"
      pattern: "postgres_changes.*bookings"
---

<objective>
Create the database foundation for Phase 4 (migration), the GPS ingest API route, and two Supabase Realtime client hooks for booking status and vehicle location subscriptions.

Purpose: Every other Phase 4 plan depends on this schema and these hooks. The migration extends the booking status CHECK, creates the vehicle_locations table, adds GPS device mapping to vehicles, adds vehicle_availability_blocks for admin scheduling, and adds is_active for admin deactivation. The GPS route bridges hardware to database. The hooks bridge database to UI.

Output: 1 SQL migration, 1 API route, 2 custom hooks
</objective>

<execution_context>
@C:/Users/humza/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/humza/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tracking-admin/04-RESEARCH.md

@lib/supabase/admin.ts
@lib/supabase/client.ts
@app/actions/admin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration — vehicle_locations, extended booking status, GPS device mapping, availability blocks, is_active</name>
  <files>supabase/migrations/20260220300000_tracking_admin_phase4.sql</files>
  <action>
Create a SQL migration file with the following changes, in this order:

1. **Extend booking status CHECK constraint** — drop existing `bookings_status_check` and re-add with all 6 values: `pending`, `confirmed`, `car_on_the_way`, `car_delivered`, `completed`, `cancelled`. Use `DROP CONSTRAINT IF EXISTS` then `ADD CONSTRAINT`.

2. **Add GPS device ID to vehicles** — `ALTER TABLE vehicles ADD COLUMN IF NOT EXISTS gps_device_id TEXT UNIQUE`. This maps a physical GPS tracker hardware device to a vehicle row.

3. **Add is_active to vehicles** — `ALTER TABLE vehicles ADD COLUMN IF NOT EXISTS is_active BOOLEAN NOT NULL DEFAULT true`. This is the admin-managed deactivation flag (distinct from `is_available` which is scraper/override managed). Deactivated vehicles are permanently removed from catalogue.

4. **Create vehicle_locations table** — upsert-only model (one row per vehicle = current location only). Schema:
   - `vehicle_id UUID PRIMARY KEY REFERENCES vehicles(id) ON DELETE CASCADE`
   - `lat DOUBLE PRECISION NOT NULL`
   - `lng DOUBLE PRECISION NOT NULL`
   - `speed_kmh NUMERIC(6, 2)`
   - `heading NUMERIC(5, 2)`
   - `recorded_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
   - `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
   Enable RLS. Add SELECT policy for authenticated users (needed for tracking page). No INSERT/UPDATE policy needed — GPS ingest uses admin client which bypasses RLS.

5. **Enable Realtime on vehicle_locations** — `ALTER PUBLICATION supabase_realtime ADD TABLE vehicle_locations`.

6. **Create vehicle_availability_blocks table** — admin-managed date blocks when a vehicle is unavailable regardless of bookings. Schema:
   - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
   - `vehicle_id UUID NOT NULL REFERENCES vehicles(id) ON DELETE CASCADE`
   - `start_date DATE NOT NULL`
   - `end_date DATE NOT NULL`
   - `reason TEXT`
   - `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
   Enable RLS. Add SELECT policy for authenticated users (availability calendar needs this). Add INSERT/UPDATE/DELETE policies for service_role only (admin actions use admin client).

Include clear comments for each section. Do NOT add columns that Phase 3 already added (kyc_status, kyc_session_id, kyc_verified_at on profiles — these already exist). Do NOT add deposit_amount to vehicles — it already exists from Phase 3.
  </action>
  <verify>Read the migration file and confirm: (1) bookings_status_check includes all 6 values, (2) vehicle_locations table has vehicle_id as PRIMARY KEY, (3) vehicle_availability_blocks table exists, (4) supabase_realtime publication includes vehicle_locations, (5) RLS enabled on both new tables.</verify>
  <done>Migration SQL file exists with correct schema for all Phase 4 database requirements. Pending user running `supabase db push`.</done>
</task>

<task type="auto">
  <name>Task 2: GPS ingest API route with shared secret auth and Zod validation</name>
  <files>app/api/gps/route.ts</files>
  <action>
Create a Next.js App Router API route at `app/api/gps/route.ts` that handles POST requests from GPS tracker hardware.

**Authentication:** Validate `x-gps-secret` header against `process.env.GPS_INGEST_SECRET`. Return 401 if missing or wrong.

**Payload validation:** Use Zod to validate the JSON body:
- `device_id: z.string()` — GPS tracker's unique hardware ID
- `lat: z.number().min(-90).max(90)`
- `lng: z.number().min(-180).max(180)`
- `speed_kmh: z.number().optional()`
- `heading: z.number().optional()`
- `timestamp: z.string().optional()` — ISO8601 from device

Return 422 with `error.flatten()` on validation failure.

**Device lookup:** Use `createAdminClient()` to look up the vehicle by `gps_device_id`. If no vehicle found, log a warning and return `{ ok: true, warning: 'Unknown device' }` (200 — prevent hardware retry loops).

**Upsert location:** Use `.upsert()` with `onConflict: 'vehicle_id'` on the `vehicle_locations` table. This keeps only the latest position per vehicle. Set `recorded_at` from the device timestamp (fallback to `new Date().toISOString()`), and `updated_at` to current time.

**Important:** The upsert triggers a Supabase Realtime UPDATE event (not INSERT) because the row already exists after the first write. The Realtime hooks must subscribe to UPDATE events accordingly.

Import `createAdminClient` from `@/lib/supabase/admin`. Import `z` from `zod`. Import `NextRequest`, `NextResponse` from `next/server`.

Return 500 on database errors. Return `{ ok: true }` on success.
  </action>
  <verify>Read the file and confirm: (1) x-gps-secret header check, (2) Zod schema validation, (3) vehicle lookup by gps_device_id, (4) upsert with onConflict: 'vehicle_id', (5) proper error handling for all paths.</verify>
  <done>GPS ingest route handles POST with auth, validation, device lookup, and upsert. Can be tested with `curl -X POST http://localhost:3000/api/gps -H "x-gps-secret: test" -H "Content-Type: application/json" -d '{"device_id":"tracker-1","lat":25.2048,"lng":55.2708}'`.</done>
</task>

<task type="auto">
  <name>Task 3: Supabase Realtime hooks — useRealtimeBooking and useVehicleLocation</name>
  <files>lib/hooks/use-realtime-booking.ts, lib/hooks/use-vehicle-location.ts</files>
  <action>
Create two client-side Realtime subscription hooks in `lib/hooks/`.

**Hook 1: `useRealtimeBooking` (`lib/hooks/use-realtime-booking.ts`)**
- `'use client'` at top
- Accept `{ bookingId: string, initialStatus: string }` as props
- Return `{ status: string }` — the current booking status
- Use `createClient()` from `@/lib/supabase/client` to create a Supabase browser client
- Subscribe to `postgres_changes` on `bookings` table, event `UPDATE`, filter `id=eq.${bookingId}`
- On payload, extract `payload.new.status` and update the returned status state
- **Critical:** Clean up with `supabase.removeChannel(channel)` in the useEffect return function to prevent channel leaks on React Strict Mode / HMR
- Define `TrackingStatus` type: `'pending' | 'confirmed' | 'car_on_the_way' | 'car_delivered' | 'completed' | 'cancelled'`

**Hook 2: `useVehicleLocation` (`lib/hooks/use-vehicle-location.ts`)**
- `'use client'` at top
- Accept `{ vehicleId: string, initialLat?: number, initialLng?: number }` as props
- Return `{ lat: number | null, lng: number | null }` — the current vehicle position
- Subscribe to `postgres_changes` on `vehicle_locations` table, event `UPDATE` (NOT INSERT — upsert triggers UPDATE), filter `vehicle_id=eq.${vehicleId}`
- On payload, extract `payload.new.lat` and `payload.new.lng`, update state
- **Critical:** Clean up with `supabase.removeChannel(channel)` in useEffect return
- Also subscribe to event `INSERT` as a fallback for the very first location write (when no row exists yet, the first upsert is an INSERT). Use `event: '*'` to catch both INSERT and UPDATE, or subscribe to both events on the same channel.

Both hooks should use `useEffect` with `[bookingId/vehicleId, supabase]` dependency array. Both must import `createClient` from `@/lib/supabase/client`.
  </action>
  <verify>Read both hook files and confirm: (1) 'use client' directive, (2) postgres_changes subscription with correct table and filter, (3) useEffect cleanup calls removeChannel, (4) correct event type (UPDATE for vehicle_locations, UPDATE for bookings), (5) proper TypeScript types for return values.</verify>
  <done>Both Realtime hooks export named functions, subscribe to the correct tables, handle INSERT/UPDATE events, and clean up channels on unmount. Ready for consumption by tracking UI components in Plan 02.</done>
</task>

</tasks>

<verification>
1. Migration file has correct SQL syntax and all required schema changes
2. GPS route validates auth, payload, looks up device, and upserts location
3. Both hooks subscribe to correct Realtime channels with proper cleanup
4. No circular imports or 'use client'/'server-only' violations
5. All files use existing project patterns (createAdminClient, createClient from correct paths)
</verification>

<success_criteria>
- Migration SQL covers: extended booking status CHECK (6 values), vehicle_locations table (PK on vehicle_id), gps_device_id on vehicles, is_active on vehicles, vehicle_availability_blocks table, Realtime publication for vehicle_locations, RLS on both new tables
- GPS ingest route accepts POST, validates shared secret + Zod schema, upserts to vehicle_locations
- useRealtimeBooking returns live booking status via Realtime subscription
- useVehicleLocation returns live lat/lng via Realtime subscription
- All hooks clean up channels in useEffect return
</success_criteria>

<output>
After completion, create `.planning/phases/04-tracking-admin/04-01-SUMMARY.md`
</output>
